#!/bin/sh

set -e 

# check apk packages installed
curl --version
git --version
jq --version
lpass --version
less --version
libressl --version
which lynx
which ssh
python  --version
make --version
node --version
tar --version
npm  --version
tmux -V
vim --version
