#!/bin/sh

set -e 

which busybox-extras
curl --version
docker -v
git --version
jq --version
lpass --version
less --version
libressl --version
which lynx
which man
nnn -v
node --version
npm  --version
which ssh
tmux -V
